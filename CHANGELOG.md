# changelog

## [0.1.0] - 2026-02-22

- add release SBOM generation and upload (CycloneDX)
- add `lint` subcommand for retort/projection consistency checks
- add plan summaries (`create/update/delete`) in `plan` output
- add interactive apply mode (`apply --interactive`)
- add projection proposal flow (`--projection-propose`) with custom field/tag creation
- add generic REST adapter backend (`--backend generic`)
- add peeringdb read-only adapter backend (`--backend peeringdb`)
- add `extract` command for backend -> canonical inventory export
- add `cast django` command for schema-driven django scaffold generation

## 0.0.1

- initial version
