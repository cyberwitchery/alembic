{
  "description": "preview_schema answers with a null result when the adapter cannot preview; the host reads it as \"no report\", not as a failure",
  "request": {
    "version": 1,
    "setup": {},
    "method": "preview_schema",
    "schema": {
      "types": {
        "dcim.site": {
          "key": {
            "site": { "type": "slug", "required": false, "nullable": false }
          },
          "fields": {
            "name": { "type": "string", "required": false, "nullable": false },
            "slug": { "type": "slug", "required": false, "nullable": false }
          }
        }
      }
    }
  },
  "response": {
    "ok": true,
    "result": null
  }
}
