{
  "description": "a request with an unsupported protocol version is rejected before the adapter runs",
  "request": {
    "version": 2,
    "setup": {},
    "method": "ensure_schema",
    "schema": {
      "types": {
        "dcim.site": {
          "key": {
            "site": { "type": "slug", "required": false, "nullable": false }
          },
          "fields": {
            "name": { "type": "string", "required": false, "nullable": false },
            "slug": { "type": "slug", "required": false, "nullable": false }
          }
        }
      }
    }
  },
  "response": {
    "ok": false,
    "error": "unsupported protocol version 2 (expected 1)"
  }
}
