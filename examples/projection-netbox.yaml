version: 1
backend: netbox
rules:
  - name: model_to_custom_fields_devices
    on_kind: dcim.device
    from_x:
      map:
        "model.fabric": "fabric"
        "model.role_hint": "role_hint"
    to:
      custom_fields:
        strategy: explicit

  - name: model_tags
    on_kind: "*"
    from_x:
      key: "model.tags"
    to:
      tags:
        source: value
